<section class="content-main">
    <div class="content-header">
        <div>
            <h2 class="content-title card-title">Categories </h2>
            <p>Add, edit or delete a category</p>
        </div>
        <div>
            <input type="text" placeholder="Search Categories" class="form-control bg-white">
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-3">
                    <form id="login-form">
                        <div class="mb-4">
                            <label for="product_name" class="form-label">Name</label>
                            <input type="text" name="name" placeholder="Type here" class="form-control" id="product_name" />
                        </div>

                       
                        <div class="mb-4">
                            <label  class="form-label">Description</label>
                            <textarea name="description" placeholder="Type here" id="desc" class="form-control"></textarea>
                        </div>
                        <div class="d-grid">
                            <button type="submit" onclick="addCategory()" class="btn btn-primary">Create category</button>
                        </div>

                    </form>
                </div>
                
                <div class="col-md-9">
                    <div class="table-responsive">
                        <table class="table table-hover" style="overflow: hidden;">
                            <thead>
                                <tr>
                                    <th class="text-center">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" value="" />
                                        </div>
                                    </th>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Order</th>
                                    <th class="text-end">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% category.forEach(function(category) { %>
                                    <tr>
                                      <td class="text-center">
                                        <div class="form-check">
                                          <input class="form-check-input" type="checkbox" value="" />
                                        </div>
                                      </td>
                                      <% var randomNum = Math.floor(Math.random() * 1223) + 1; %>
                                      <td><%= randomNum %></td>
                                      <td><b><%= category.name %></b></td>
                                      <td><%= category.description %></td>
                                      <% var randomNum2 = Math.floor(Math.random() * 102); %>
                                      <td><%= randomNum2 %></td>
                                      <td class="text-end">
                                        <div class="dropdown">
                                            <a href="#" data-bs-toggle="dropdown"
                                                class="btn btn-light rounded btn-sm font-sm"> <i
                                                    class="material-icons md-more_horiz"></i> </a>
                                            <div class="dropdown-menu">
                                                <a class="dropdown-item" href="#">View detail</a>
                                                <a class="dropdown-item" href="#">Edit info</a>
                                                <a class="dropdown-item text-danger" onclick="deleteCategory('<%= category._id %>')"
                                                href="#">Delete</a>
                                            </div>
                                        </div>
                                    </td>
                                    </tr>
                                  <% }) %>

                            </tbody>
                        </table>
                    </div>
                </div> <!-- .col// -->
            </div> <!-- .row // -->
        </div> <!-- card body .// -->
    </div> <!-- card .// -->
</section>


<script>
    function addCategory() {
        const description = document.getElementById('desc').value.trim()
        const name = document.getElementById('product_name').value.trim()
        $.ajax({
      url: '/admin/categories',
      method:'POST',
      data:{
        name:name,
        description:description
      },
      success:(resp)=>{
        alert('Category created successfully');
      },
      error:()=>{
        alert('Something went wrong.Cant add category');
      }
    })
    }

    function deleteCategory(categoryId) {
        const description = document.getElementById('desc').value.trim()
        const name = document.getElementById('product_name').value.trim()
        $.ajax({
      url: '/admin/categories',
      method:'PUT',
      data:{
        name:name,
        description:description
      },
      success:(resp)=>{
        
      },
      error:()=>{
        alert('Something went wrong.Cant delete category');
      }
    })
    }

</script>
